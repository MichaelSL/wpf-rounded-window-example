<Window x:Class="RoundedForm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoundedForm"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="150"
        Width="150"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown" Icon="icon.png">
    <Window.Resources>
        <ImageBrush x:Key="imgBrush" ImageSource="icon.png" />
    </Window.Resources>
    <Border CornerRadius="200" 
            Padding="0" 
            Margin="0" 
            BorderBrush="#99FFC0C0" 
            BorderThickness="1" 
            Background="{StaticResource imgBrush}">
        <Grid>
            
        </Grid>
    </Border>
</Window>
